# V5.1.3 界面精简与验收

## 界面调整

- 投入分析驾驶舱保留一组统计卡片，与类别、部门图表联动。
- 投入计划列表移除顶部卡片，申请金额合计放在表尾并随筛选变化。
- 审批与财务确认直接显示两个队列页签和待办数，不再重复展示统计卡片。
- 调整台账移除顶部卡片，表尾显示已生效调整笔数与变动净额。
- 执行台账移除顶部卡片，核定、计划付款、已核销和剩余额度改为表尾一行合计。
- 预警中心直接展示可筛选的等级、处理状态和预警记录。
- 计划详情、调整详情和核销流水弹窗使用紧凑字段信息，不再堆叠卡片。
- 保留 V5.1.2 的业务增强、权限检查和防重复提交等行为。

## 本地验证（2026-08-31）

- 隔离 SQLite 数据库全量回归：64 项 Python 测试通过。
- 前端测试：8 组通过，包括卡片仅出现在分析驾驶舱、紧凑信息转义、路由、复合筛选、导出保护及布局约束。
- 使用经用户授权的本地管理员账号登录，检查投入管理七个菜单页面和计划详情，未出现页面加载失败。
- 实测计划年度筛选：切换到 2027 年草稿后，表尾申请金额合计变为 ¥0.00；重置后恢复。
- 1280 像素宽度逐页检查：页面宽度未超出视口；只有分析驾驶舱出现一组统计卡片。
- 1024 像素宽度检查预警规则、执行台账和核销流水弹窗：无页面级横向溢出，预警保存按钮未越出规则容器。
- 查看核销流水弹窗及审批待办空状态；未通过浏览器提交付款或修改线上业务数据。
- 数据回归使用临时测试库，不包含在交付包中；工程 data 目录与 V5.1.2 保持一致。
- 未连接线上 Neon，不宣称完成线上 PostgreSQL 或第三方集成联调。审批、调整、核销业务由自动化测试验证。

## 更新到 Render

1. 将本工程文件夹内的内容更新到 GitHub 仓库根目录。
2. 等待 Render 部署完成，再刷新网页。静态资源版本已更新为 5.1.3，避免旧脚本、样式缓存混用。
3. 保留既有 DATABASE_URL 及 AI/MCP/TAPD 环境变量，无须新增变量、重建数据库或重新迁移数据。

本交付未推送 GitHub、未部署 Render、未操作原服务器。压缩包不含 .env、虚拟环境或临时测试数据库。

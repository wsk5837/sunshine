# V5.1.1 首页与投入管理加载修复

## 修复内容

- 修正首页引用的投入脚本、样式地址，统一通过已有的 `/static/` 路由加载。
- 前端资源版本更新为 `5.1.1`，部署后使用新 URL 获取脚本和样式。
- 投入页面函数仅在进入对应页面时解析。投入脚本异常时，仅投入页面显示重试入口，不再导致首页、需求或项目页面一起加载失败。
- 保留原有 `/app.js`、`/app.css` 路由，兼容尚未刷新的旧页面。
- 不调整业务规则、权限、数据库结构、数据库内容或现有环境变量。

## 更新现有 GitHub + Render 服务

1. 将本文件夹的工程文件放到现有 GitHub 仓库根目录，提交更新；不要把整个文件夹再套一层。
2. 不上传本机 `.env`、`.venv`、密码或密钥；保留 Render 当前的 `DATABASE_URL`、AI、MCP、TAPD 配置。
3. 等待 Render 自动部署成功。若没有自动部署，手动部署最新提交即可，无需新建服务、重建数据库或重复迁移。
4. 打开系统并刷新。若浏览器仍显示旧页面，使用强制刷新（Mac：⌘⇧R；Windows：Ctrl+F5）。
5. 分别检查首页、投入分析驾驶舱、投入计划编制，再打开需求和项目列表。

本次未推送 GitHub，也未操作线上服务器或数据库。

## 验证

本次全量回归结果：**58 项通过**；前端路由测试包含 3 组场景，覆盖全部 8 条投入页面路由。

- JavaScript 语法检查。
- 按 Render 实际启动入口请求首页，逐一请求 HTML 引用的脚本与样式，检查 HTTP 状态、内容类型和非空内容。
- 执行真实路由代码，验证投入脚本缺失时首页/需求/项目隔离、全部 8 条投入路由重试提示及加载后的函数分发。
- 独立临时 SQLite 数据库进行全量回归测试，不使用交付快照或线上数据库。
- 本机浏览器访问被工具策略拦截，未完成浏览器目视验收；上线后请按上述步骤检查页面。

测试命令（先安装 `requirements.txt`；前端路由测试另需 Node.js）：

```sh
node --test tests/frontend_routes.test.mjs
TRM_DATABASE_BACKEND=sqlite TRM_DB_PATH=/tmp/trm-v511-new-test.db python -m pytest -q
```

回归测试请使用新的临时数据库文件，不要设置为业务数据库路径。

# TRM V5.1.3 可交付文件说明

## 启动入口

- macOS：双击 `启动系统.command`
- 命令行：执行 `start.sh`
- Docker：执行 `docker compose up --build`

启动后访问：`http://127.0.0.1:8000`

## 核心源码

- `app/main.py`：FastAPI 主应用、需求全生命周期和统一 AI 接口
- `app/investment.py`：数字化投入计划、审批、调整、核销、预警和统计分析接口
- `app/ai_gateway.py`：Gazellio G.AIOS 智能体适配与 NDJSON 解析
- `app/auth.py`：登录、用户、角色和权限
- `app/extended.py`：立项、项目、360视图、预算、合同和结算
- `app/poc.py`：审批、OA、TAPD 和 POC 闭环
- `app/v4.py`：集成配置、风险、交付物等平台能力
- `app/static/`：网页界面、样式和交互脚本，其中 `investment.js` / `investment.css` 是投入管理工作台

## 配置与部署

- `.env.example`：环境变量示例，不包含真实密码或管理员 Token
- `requirements.txt`：Python 依赖
- `Dockerfile`、`docker-compose.yml`：容器部署
- `render.yaml`：免费 Render + Neon PostgreSQL 持久化配置
- `render-persistent.yaml`：付费 Render 磁盘 + SQLite 备选配置
- `NEON_RENDER_DEPLOY.md`：试点部署与首次数据迁移说明
- `RENDER_V5.1_DEPLOY.md`：V5.1 新 GitHub 仓库 + Render 部署步骤
- `V5.1_CHANGELOG.md`：数字化投入管理新增功能与验证结果
- `V5.1.1_修复与更新说明.md`：首页/投入模块加载修复与现有 Render 服务更新步骤
- `V5.1.2_投入管理更新说明.md`：投入管理布局、业务增强及测试范围
- `V5.1.3_界面精简与验收.md`：移除重复统计卡片、浏览器实测和更新说明

## 数据目录

- `data/`：当前 SQLite 数据快照；Neon 试点首次部署自动迁移
- `uploads/`：业务附件

交付包未包含本机 `.venv`、测试数据库、Python 缓存或其他临时文件。

# V5.1.2 投入管理优化

## 布局修正

- 所有投入表格使用系统统一表格类，撑满卡片宽度，表头、行间距和分隔线完整；金额右对齐且不挤在一起。
- 超宽表格在卡片内部横向滚动，不扩张整个页面。
- 筛选条件使用紧凑的多列布局，根据可用宽度换行。
- 标题和操作按钮统一对齐；空状态占用合理高度，操作按钮禁止拆字。
- 审批和财务队列改为页签，分类和预警配置改为页签；不再把两个窄面板并排挤压。
- 预警规则采用卡片表单，比例显示百分比单位，天数显示天数单位，不显示该规则无关的零值字段。
- 弹窗内的双列表单使用等宽列，日期、数量、金额保留原生选择和输入校验。
- 样式仅作用于投入页面与投入弹窗，不改动其他业务模块的样式。

## 业务操作增强

1. 计划、明细、调整、核销和预警台账支持关键词及多条件筛选、列排序、10/20/50条分页和筛选结果CSV导出。导出中的文本进行公式注入防护。
2. 驾驶舱指标和类别/部门图表随筛选结果联动；已生效额度仅统计已生效投入，草稿不冒充已审批预算。
3. 计划编制可主动读取同部门历史已生效投入预算与核销金额；没有历史记录时保留人工填写值，不生成虚构参考数据。
4. 审批队列显示选中数量，未勾选时禁用批量操作。单条处理可逐项核定金额，提交后更新明细和计划汇总；驳回需填写原因。批量处理显示失败条目的原因。
5. 重复的批量计划ID被去重，避免一次请求推进多个审批节点。
6. 调整申请支持保存草稿、编辑草稿或被驳回的申请并重提；详情展示金额和范围前后对比、调整原因及审批记录。
7. 调整终审再次检查最新核销金额和基线，防止审批期间数据变化后错误覆盖。
8. 执行台账展示计划付款、核定额度、已核销、剩余和执行率。新增核销流水弹窗，能按年度和类型筛选并导出单据。
9. 合同选项随关联项目过滤。付款年份由日期自动推导，金额不得超过剩余额度，重复提交期间禁用操作按钮。
10. 预警中心可查询待处理、已处理、已恢复的记录。相同风险被标记已处理后不会因刷新立即重新打开；风险内容或等级改变，以及恢复后再次出现时重新触发。
11. 长期未执行预警优先使用财务确认生效时间计算，而不是计划明细的创建日期。
12. 调整查看、编辑、审批及预警处理使用区分后的后端权限；审批操作仍遵守当前节点与角色约束。

## 更新部署

- 将本文件夹内工程内容更新到现有 GitHub 仓库根目录，等待 Render 部署成功后刷新页面。
- 继续保留 Render 中已设置好的 `DATABASE_URL` 和其他 AI/MCP/TAPD 变量，不要提交真实密码或 `.env`。
- 本次没有新增数据库表或更改连接配置，不需要重建 Neon 数据库或重新导入数据。
- 工程内已有数据库快照未被此次测试修改。测试数据仅在独立临时 SQLite 文件中。
- 没有推送 GitHub，也没有操作线上 Render、Neon 或服务器。

## 验证范围

- Python 全量回归：64项通过，包含6项新增投入工作台业务测试。
- 前端脚本：语法检查通过；路由、模块隔离、复合筛选、CSV防公式注入、表格样式约束等6组JavaScript测试通过。
- 首页实际引用的脚本和样式均通过HTTP资源可达性测试。
- 本次数据库业务回归使用SQLite，未连接用户的线上Neon数据库。
- 本地浏览器已打开登录页，但自动登录测试账号被安全机制拦截，等待用户授权；未完成登录后的视觉及点击验收。本说明不将自动化测试等同于浏览器验收。

## 建议上线后的验收顺序

1. 投入计划：切换年度、部门和状态，查看分页、排序与导出结果。
2. 新建计划及明细：检查输入框和日期控件；核对提交后进入待办队列。
3. 审批：单条核定金额，再批量通过/驳回；核对审批记录、核定汇总与财务确认。
4. 调整：保存草稿、提交、驳回、编辑重提；查看范围对比与历史记录。
5. 执行：关联项目/合同，登记一笔测试付款，核对余额和核销流水。
6. 预警：标记一条已处理，刷新确认状态保留；在配置页检查规则单位和保存结果。
7. 在常用电脑分辨率下检查卡片内滚动及按钮位置，确认没有页面级横向溢出。

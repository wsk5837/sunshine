# V4.7 目录重整说明

- 恢复 `app/` Python 包结构。
- 恢复 `app/static/` 前端静态资源结构。
- `main.py`、`db.py`、`extended.py`、`v4.py`、`poc.py`、`rules.py` 全部保留 V4.6 最新业务代码。
- 保留 V4.6：首页增强、重复百分比修复、TAPD Mock/Live、连接测试、任务/工时回读、同步日志与重试能力。
- Python 内部引用恢复为相对包导入，例如 `.db`、`.rules`。
- Render 启动路径永久统一为 `app.main:app`。
- `Root Directory` 应留空。
- 新增 `.gitignore`，避免 `.venv`、`.idea`、`__pycache__`、`.pyc`、本地数据库等被误上传。
- 不再使用扁平目录结构。


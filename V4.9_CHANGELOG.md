# V4.9 AI智能体对接版变更说明

## 功能点台账与逐功能点预算分摊

- 需求内新增、导入和编辑的功能点会同步进入统一功能点库；历史功能点在启动升级时自动补齐目录关联，功能点管理不再只有4条预置数据。
- 功能点管理展示实际功能点编号、目录编号、已关联需求数量及需求编号，并避免向同一需求重复关联。
- 预算分摊改为强制关联功能点，分摊金额按“功能点评估金额 × 该行比例”计算，不再使用需求或项目总金额作为统一基数。
- 同一功能点可拆分到多个预算项；保存时单功能点合计不得超过100%，产品经理和财务审批前要求每个功能点分别达到100%。
- 功能点预算已占用后禁止修改评估金额或删除，避免预算流水与评估台账失配。

## 新增

- 新增 `app/ai_gateway.py`，服务端调用 Gazellio G.AIOS `/adk/run_stream` 并解析 NDJSON。
- 新增 `POST /api/ai/chat` 统一对话接口及 `GET /api/ai/config` 非敏感配置接口。
- 项目360机器人接入真实智能体，自动携带所选项目的只读事实快照。
- AI智能问答页面支持 G.AIOS 多轮 `session_id` 会话。
- 新增全局 AI 助手悬浮球，可在任意登录后的业务页面对话。
- AI集成配置新增公开 `agent_id` 字段与只读连通性检查。
- 新增外部智能体异常时的 TRM 本地知识引擎降级。
- 新增智能体代理、会话、项目上下文和思考内容过滤测试。
- 新增 macOS `启动系统.command`，可双击安装依赖、启动服务并打开系统。
- 前端资源改为兼容相对路径；误用文件方式打开时显示明确启动提示，不再出现未加载样式的大图标页面。
- 新增 `app/trm_mcp.py` 标准 Streamable HTTP MCP服务，G.AIOS可发现13个 `trm_*` 工具。
- 工具覆盖预算、需求、项目查询与360详情，以及受控创建需求草稿/项目。
- 新增 `scripts/check_mcp.py` 只读联调脚本和 `config/gaios-mcp-config.example.json` 后台配置示例。
- 系统管理–集成配置新增 TRM MCP 就绪状态卡片。

## 安全

- 前端不直连 G.AIOS，不保存后台用户名、密码或管理员 Token。
- G.AIOS `agent_id` 由后端配置并固定，用户不能在请求中任意指定智能体。
- 每次智能体调用均写入集成日志和操作审计。
- 模型事件中标记为 `thought=true` 的内部思考内容不会返回给前端。
- MCP端点使用独立Bearer Token，不复用G.AIOS后台密码。
- MCP创建工具强制“预览+短期签名确认令牌+用户级幂等键+服务端写开关”。
- AI操作权限与现有业务权限完全同步，不再设置独立的 `ai.query.*` / `ai.create.*` 权限。预算、需求、项目查询及创建能力直接继承 `budget` / `demand.list` / `project360|project` / `demand.create` / `initiative.create|project`。
- TRM为当前登录用户签发短时AI委托令牌；13个MCP工具每次执行都校验会话、账号、角色及实时权限。
- 退出登录、停用账号/角色或撤销对应业务权限会立即重算用户权限并集，AI已发放但已无对应业务权限的能力随即失效。
- AI创建需求时申请人姓名、账号和部门由服务端按登录用户强制绑定，模型不能伪造。
- MCP业务审计记录真实用户、用户角色、所需权限、委托ID和集成服务身份。
- MCP所有操作与前端API共用同一数据库，不接受模型传入角色来提升权限。

## 多角色与真实RBAC

- 新增 `system_user_roles` 用户-角色多对多关系表，用户管理界面支持同时勾选多个角色。
- 登录、`/api/auth/me` 和用户管理 API 返回 `role_codes` / `role_labels` 及合并后的实时权限。
- 新增后端 API-权限映射与统一鉴权中间件，将菜单权限升级为真实的服务端功能权限。
- 审批待办和审批节点支持多角色匹配，不再只判断一个 `X-Role`。
- 修改角色权限后，现有登录会话、页面菜单、业务 API 和 AI/MCP 工具在下一次请求立即同步。
- 新增自动化测试，覆盖多角色权限并集、后端越权 `403`、不重新登录实时撤权，以及旧 AI/MCP 委托令牌同步失效。

## 预警与计算交互修复

- 工时偏差率超过 30% 时，预警会持久化到消息中心，不再只显示“已发送”的静态文案。
- 产品经理和项目经理各收到一条定向预警，历史数据可自动补齐并防止刷新重复发送。
- 需求详情根据实际消息记录显示发送状态，并明确说明定向消息的角色可见性。
- 新增可点击的预估功能点汇总，直达功能点评估页签。
- 预估金额改为鼠标悬停/键盘聚焦显示功能点计算明细，需求基本信息、功能点汇总和产品评估界面统一生效。
- 自动化测试增加预警持久化、角色隔离、防重复和悬停计算交互合约。

## 首页按权限降级

- 修复需求申请人进入首页时，因无权请求审批待办接口而导致整页加载失败的问题。
- 没有 `demand.approve` 权限时，前端不发起待办请求，并完全隐藏审批待办模块。
- TAPD指标、首页业务板块和快捷入口均按实时权限独立渲染，任一模块无权不再影响整个首页。

## 全站无权模块隐藏

- 建立统一的路由-权限映射，覆盖全部菜单路由、详情路由和表单路由。
- 无权菜单、空菜单组、页面内子模块、操作按钮及跨模块关联字段直接隐藏。
- 无权路由和动态撤权统一静默回退到可访问页面，不再输出未授权失败页。
- TAPD 页面不再为普通同步用户请求管理员配置接口，并将连接配置整块限定为 `system.integrations`。
- 项目360机器人、需求详情功能点/预算/TAPD页签、用户角色筛选及关联项目/预算/合同字段均完成模块级权限隔离。

## 导出与下载黑屏修复

- 修复首页“导出汇总”直接打开 API 时无法携带 `X-Session`，导致新页面显示 `AUTH-4010` JSON 的问题。
- 统一将首页汇总、附件、功能点模板及功能点导出改为携带当前会话的 Blob 下载，不再跳离 SPA。
- 下载期间按钮自动防重复点击；会话过期时返回登录页，无权时同步刷新菜单与页面。
- 增加全站按钮静态绑定审计及受保护下载回归测试。
- 回归测试验证需求申请人可正常读取首页数据、审批待办仍被后端 `403` 拒绝，以及前端会跳过该请求并隐藏模块。

## AI展示与指标库优化

- 清理登录页、首页、审批页、AI问答页和集成配置中面向开发/演示的说明性文案。
- AI回答新增安全 Markdown 渲染，支持标题、列表、粗体、链接、引用和标准表格，不再原样显示 `**` 或表格竖线。
- AI问答页、项目360助手和全局悬浮助手共用统一回答组件与样式。
- 更新 G.AIOS 系统提示词和 MCP 服务输出规范：结论在前、列表优先、仅对比数据使用标准表格，不直接输出原始 JSON 或工具实现说明。
- 指标库扩充为15项预置指标、10个管理维度和4期历史趋势，覆盖交付、流程、预算、质量、风险、项目、合同、AI运营与系统运行。
- 指标新增“越高越好/越低越好”目标方向，达成度、临界和预警状态按口径正确计算。
- 指标维护新增统计概览、搜索和分类/健康筛选；指标数据和看板新增趋势图、分类展示与达成状态。

## 真实业务闭环补强

- 工时计划由产品经理/项目经理真实维护，实际工时改为逐笔登记、删除和自动汇总，来源与维护人可追溯。
- 工时超支率只计算实际超出预估的正向比例；超支超过30%和计划逾期均写入消息中心，并可在条件解除后自动撤销旧预警。
- TAPD回读以工时填报为实际工时权威来源，无填报时才回退任务进度；多TAPD需求的数据按外部需求隔离，避免互相覆盖。
- MCP新增工时计划和实际工时各一组“预览+确认写入”工具，页面与AI实时共享 `demand.evaluate|project` 权限。
- 项目任务的新增、更新和删除会自动汇总项目进度；预算、结算、合同付款补齐金额、日期、状态及余额校验，阻止流程无数据落账。
- 后端全量回归覆盖工时、权限、预算、结算、项目进度、TAPD、导出下载和AI/MCP；当前累计46项测试通过。

## 消息中心优化

- 同一预警按角色分别投递、按业务事件合并展示，多角色用户和管理员不再看到两条完全相同的消息。
- 消息卡片新增严重程度、需求编号与标题、接收角色、相对时间、未读/已读/已恢复状态和需求详情跳转。
- 已读状态改为按用户保存，不再因一名产品经理查看消息而影响同角色其他用户。
- 新增“全部已读”；预警解除后保留历史记录并标记“已恢复”，同类风险再次发生时可生成新事件。

## TAPD详情精简

- 移除没有人工维护入口、且不属于POC验收核心页面的“需求关联任务信息”和“任务关联花费信息”展示区。
- “TAPD需求创建结果”调整为紧凑的“TAPD同步状态”，仅在真实存在TAPD记录时显示。
- “需求信息回读”精简为状态映射、研发负责人、研发部门、上线日期和同步来源，仅有回读数据时显示。
- 无TAPD数据、无同步记录时不再渲染大面积空白卡片；底层任务与工时回读仍用于TAPD状态和实际工时自动汇总。

## 指标看板大屏化

- 指标看板由“按分类纵向堆叠大卡片”重构为一屏式管理驾驶舱，显著缩短页面高度。
- 顶部集中展示监测指标、综合达成度、已达标和重点关注四项核心统计。
- 中部并排呈现管理维度达成度、低达成度关键指标趋势和健康度分布。
- 底部使用五列紧凑指标矩阵承载全部15项指标，保留当前值、目标值、更新频率和达成度。
- 增加桌面、窄屏和移动端自适应布局，常见PC分辨率优先保持大屏观感。

## 项目合同与结算一体化

- 项目编辑新增合同、结算关联工作台，可直接勾选尚未关联其他项目的已有单据。
- 项目内支持新建合同和新建结算，创建时自动写入项目编号、预算来源，并实时同步合同台账和结算台账。
- 项目详情的合同、结算板块增加“关联 / 新建”和台账详情入口，不必在多个菜单间反复切换。
- 关联关系通过后端事务真实更新合同、结算的 `project_id`；已被其他项目占用的单据禁止重复关联，操作写入审计日志。
- 项目台账新增来源列，区分“立项审批生成”和“存量录入”。
- 常规建设项目统一从“发起立项”进入审批流程；原“新建项目”调整为“录入存量项目”，仅用于已完成线下审批或历史数据迁移。

## 立项审批自动建项

- 分管领导完成最后一级审批后，系统在同一事务中自动创建项目编号并写入项目台账。
- 取消立项详情的“生成项目”人工操作；已建项后仅提供“查看项目”入口。
- 自动建项按立项编号幂等执行，审批或旧版客户端重试不会生成重复项目。
- 项目来源统一显示为“立项审批生成”，不再使用容易误解的“立项转项目”。

## 首页操作区精简

- 移除首页标题区右上角的“新建需求”和“导出汇总”按钮，让驾驶舱聚焦数据概览。
- 新建需求仍可从需求管理和首页快捷入口进入；导出功能保留在对应的查询与报表页面。

## 全局表单填写助手

- 系统内所有 `date` / `datetime-local` / `time` 输入项统一增加显式日历按钮，点击直接打开浏览器选择器，无需记忆日期格式。
- 需求描述、立项背景/目标/范围/收益、项目说明、风险措施、合同/结算说明和审批意见等长文本字段自动提供“AI草拟 / AI润色”。
- AI会结合当前表单已填内容生成建议，仅在预览区展示；用户点击“使用此内容”后才回填，不自动覆盖或保存业务数据。
- 填写助手通过后端调用已配置的 Gazellio G.AIOS 智能体，不在前端暴露凭据；智能体暂时不可用时启用不编造业务事实的本地草拟兜底。
- AI填写按钮严格跟随当前用户的 `ai` 权限，管理员关闭权限后不再渲染。

## TAPD双向Live、预算分摊与工时审批

- TAPD Live终审创建使用REQ编号保证幂等，保存真实TAPD ID、需求URL、创建时间和同步状态。
- TRM可将标题、描述、优先级、计划完成日期和预估工时主动回写TAPD；手动、定时同步及Webhook可将状态和工时回读TRM。
- TAPD Webhook改为免TRM登录会话、强制独立密钥校验，解决外部回调被系统登录鉴权拦截的问题。
- 预算管理新增统一费用分摊台账及人力/财务项目概览；费用分摊按最新评估金额重算，财务审批真实占用预算，后续驳回自动释放。
- 实际工时必须关联需求功能点并进入产品经理/项目经理审批；只有审批通过的工时计入需求实际工时和30%超支预警。
- 修复需求详情“TAPD与工时”页签角色判断变量缺失导致的页面加载失败，并为前端静态资源增加版本号避免部署后读取旧缓存。

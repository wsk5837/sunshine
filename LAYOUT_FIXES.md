# V4.2 界面适配修正

本版本针对实际运行中发现的文字遮挡、表格末列操作被裁切、左侧系统标题显示不完整等问题做了全局修正：

- 左侧导航宽度调整为 224px，并缩减标题字号和左右内边距，保证“集团全域科技资源管理驾驶舱”完整显示。
- 所有 Grid/Flex 主业务容器增加 `min-width: 0`，避免子元素撑破卡片后被裁切。
- 表格取消固定 760px 最小宽度，常规桌面宽度优先自适应；较窄窗口才启用横向滚动兜底。
- 表格单元格允许正常换行，长标题不再强制单行省略；编号、状态等仍保持单行。
- 含操作按钮的表格将操作列吸附在右侧，查看详情/编辑/删除等按钮不会被滚动区域遮住。
- 操作按钮组允许合理换行，避免多个操作按钮被截断。
- 首页“近期需求”改成紧凑表格，只展示首页真正需要的核心字段，完整字段仍在需求列表/详情中展示。
- 1320px 以下自动将双栏业务区改为单栏，防止表格和侧边业务卡片互相挤压。
- 1500px 以下同步收紧顶部栏和表格间距，避免用户名、动态文案、业务内容互相覆盖。

